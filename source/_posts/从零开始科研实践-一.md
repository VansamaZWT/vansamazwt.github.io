---
title: 从零开始科研实践(二)--半监督学习算法实践
date: 2023-11-12 12:59:30
tags: 技术 机器学习
cover: https://picx.zhimg.com/80/v2-c098cde59776255d68435414b1f44dad_720w.webp?source=1def8aca
top_image: https://picx.zhimg.com/80/v2-c098cde59776255d68435414b1f44dad_720w.webp?source=1def8aca
---
# 前言
本周科研实践的学习任务是复现上次任务知识图谱中的一个算法。从之前的调研中粗略感知，半监督学习算法难度适中，前景可观（事实上半监督学习对于数据有一定要求，这在应用中往往难以实现，故半监督学习在实际应用效果有限），决定选择一个半监督算法来学习。
# 半监督学习算法实现
![](https://picx.zhimg.com/80/v2-c098cde59776255d68435414b1f44dad_720w.webp?source=1def8aca)
**半监督学习**是一种机器学习方法，用来处理少量有标签数据和大量无标签数据的训练情况。本次科研实践任务是完成半监督学习的算法实现。
## Self-training实践
**self-training**是一种最简单的半监督学习，其思想十分简单，即通过有标签样本训练分类器（此分类器大概率是过拟合的，因为样本较少），再通过分类器为无标签数据预测标签，取其中置信度最高的部分直接贴上预测标签，加入有标签数据集，再次训练，反复迭代直到收敛。
### Demo
先用一个demo感受一下self-training
```python
import numpy as np
from sklearn import datasets
from sklearn.semi_supervised import SelfTrainingClassifier
from sklearn.svm import SVC
from sklearn.model_selection import train_test_split
from sklearn.metrics import classification_report

rng = np.random.RandomState(42)
iris = datasets.load_iris()
random_unlabeled_points = rng.rand(iris.target.shape[0]) < 0.3
iris.target[random_unlabeled_points] = -1
svc = SVC(probability=True, gamma="auto")
self_training_model = SelfTrainingClassifier(svc)
self_training_model.fit(iris.data, iris.target)
print(classification_report(datasets.load_iris().target, iris.target))
```
在上述代码中，我们调用了sklearn库中提供的数据集digits，其中，我们只给不到30%的样例提供标签，使用sklearn自带的基于SVG的self-training进行训练，得到了对应的模型。输出预测标签和真实标签的精度对比，结果比较乐观，查准度达到惊人的100%，而查全度也还不错。
[![piYV1N8.png](https://z1.ax1x.com/2023/11/14/piYV1N8.png)](https://imgse.com/i/piYV1N8)
### Self-training算法实现
![](https://img-blog.csdn.net/20180508194823170?watermark/2/text/Ly9ibG9nLmNzZG4ubmV0L3R5aDcwNTM3/font/5a6L5L2T/fontsize/400/fill/I0JBQkFCMA==/dissolve/70/gravity/SouthEast)
#### 训练模型
使用了SVM训练模型，SVM模型的具体内容暂时还没有学习，碍于时间，这里不作介绍，着重介绍self-training算法本身。
#### 引用
主要是sklearn，他提供了数据库和SVC模型，还使用了numpy来方便编程
```python
import logging
from copy import deepcopy
from sklearn.datasets import load_iris
from sklearn.metrics import accuracy_score
from sklearn.model_selection import train_test_split
import sys
import numpy as np
from SVM import SVM

```
#### 类定义与初始化方法
主要的成员属性是
*threshold*:允许的概率阈值
*max_iter*:最大迭代次数
```python
class SelfTrainingClassifier(object):
    def __init__(self,
                 base_estimator,
                 threshold=0.75,
                 max_iter=10):
        self.base_estimator_ = base_estimator
        self.threshold = threshold
        self.max_iter = max_iter
```
#### 拟合部分
总代码如下
```python
def fit(self, X, y):

        has_label = y != -1  # -1（代表没有label） 的是False 非-1（有label）的是True，
        # 有label的地方为True，无label的地方为False

        self.transduction_ = np.copy(y)  #
        self.labeled_iter_ = np.full_like(y, -1)  # 用来记录每个无标签样本被拟合的次数
        self.labeled_iter_[has_label] = 0
        self.n_iter_ = 0

        if not hasattr(self.base_estimator_, "predict_proba"):
            msg = "base_estimator ({}) 需要实现 predict_proba 方法来返回每个样本的预测概率!"
            raise ValueError(msg.format(type(self.base_estimator_).__name__))

        while not np.all(has_label) and (self.max_iter is None or
                                         self.n_iter_ < self.max_iter):
            # 如果(存在标签为-1的情况)且(最大迭代次数为空 或 当前迭代次数小于最大次数）
            self.n_iter_ += 1  # 累计迭代次数
            self.base_estimator_.fit(X[has_label],  # 取有标签的部分进行模型拟合
                                     self.transduction_[has_label])
            # ①先对每个没有标签的样本进行预测
            prob = self.base_estimator_.predict_proba(X[~has_label])  # 取没有标签的样本进行预测
            
            # ②以最大概率（这个最大概率可能没有超过设定的阈值）给一个初始的预测结果
            pred = self.base_estimator_.classes_[np.argmax(prob, axis=1)]  # 根据概率取预测标签
            max_proba = np.max(prob, axis=1)  # 取每个样本类别预测中的最大概率
            # ③选择预测概率超过阈值的样本索引
            selected = max_proba > self.threshold  # 通过阈值来进行确定哪些标签的预测结果是可信的
            selected_full = np.nonzero(~has_label)[0][selected]
            # ④将样本预测概率最大值对应的标签且概率同时大于阈值的预测结果更新到标签结果中
            self.transduction_[selected_full] = pred[selected]  # 更新标签
            has_label[selected_full] = True  # 设定原本无标签的对应样本为有标签状态
            self.labeled_iter_[selected_full] = self.n_iter_
            # labeled_iter_记录样本被拟合的次数，因为存在概率小于阈值的情况，所以可能需要拟合多次

            if selected_full.shape[0] == 0:
                # no changed labels
                self.termination_condition_ = "no_change"  # 结束标志：没有变化
                break

            print(f"第 {self.n_iter_} 次迭代结束后,"
                  f"有 {selected_full.shape[0]} 个未标记样本增加了新标签.")

        if self.n_iter_ == self.max_iter:
            self.termination_condition_ = "max_iter"  # 结束标志：达到最大迭代次数
        if np.all(has_label):
            self.termination_condition_ = "all_labeled"  # # 结束标志：所有样本都有了标记

        # 最后利用所有有标签的样本进行一次模型的拟合
        self.base_estimator_.fit(X[has_label], self.transduction_[has_label])
        self.classes_ = self.base_estimator_.classes_
        return self
```
代码很长，我们分段理解。
传入参数中X是样本的array，Y是标签的array
```python
 has_label = y != -1  # -1（代表没有label） 的是False 非-1（有label）的是True，
        # 有label的地方为True，无label的地方为False

        self.transduction_ = np.copy(y)  #
        self.labeled_iter_ = np.full_like(y, -1)  # 用来记录每个无标签样本被拟合的次数
        self.labeled_iter_[has_label] = 0
        self.n_iter_ = 0
```
这一段代码主要在初始化迭代过程中的关键变量，其中
has_label是一个存放布尔值的NUMPY.array,记录了有标签样本的索引
n_iter_是当前迭代次数
```python
while not np.all(has_label) and (self.max_iter is None or
                                         self.n_iter_ < self.max_iter):
            # 如果(存在标签为-1的情况)且(最大迭代次数为空 或 当前迭代次数小于最大次数）
            self.n_iter_ += 1  # 累计迭代次数
            self.base_estimator_.fit(X[has_label],  # 取有标签的部分进行模型拟合
                                     self.transduction_[has_label])
            # ①先对每个没有标签的样本进行预测
            prob = self.base_estimator_.predict_proba(X[~has_label])  # 取没有标签的样本进行预测
            
            # ②以最大概率（这个最大概率可能没有超过设定的阈值）给一个初始的预测结果
            pred = self.base_estimator_.classes_[np.argmax(prob, axis=1)]  # 根据概率取预测标签
            max_proba = np.max(prob, axis=1)  # 取每个样本类别预测中的最大概率
            # ③选择预测概率超过阈值的样本索引
            selected = max_proba > self.threshold  # 通过阈值来进行确定哪些标签的预测结果是可信的
            selected_full = np.nonzero(~has_label)[0][selected]
            # ④将样本预测概率最大值对应的标签且概率同时大于阈值的预测结果更新到标签结果中
            self.transduction_[selected_full] = pred[selected]  # 更新标签
            has_label[selected_full] = True  # 设定原本无标签的对应样本为有标签状态
            self.labeled_iter_[selected_full] = self.n_iter_
            # labeled_iter_记录样本被拟合的次数，因为存在概率小于阈值的情况，所以可能需要拟合多次

            if selected_full.shape[0] == 0:
                # no changed labels
                self.termination_condition_ = "no_change"  # 结束标志：没有变化
                break

            print(f"第 {self.n_iter_} 次迭代结束后,"
                  f"有 {selected_full.shape[0]} 个未标记样本增加了新标签.")
```
这段代码是迭代的主体部分。
每次迭代中，
使用我们的SVM拟合有标签部分，得到新一次迭代的分类器。
使用该分类器预测无标签部分，并得到每个无标签样本可能对应的标签及其预测概率。
将样本预测概率最大值对应的标签且概率同时大于阈值的预测结果更新到标签结果中。
重复上述步骤直到不再更新或达到最大迭代次数。
#### 预测部分
该部分直接调用了SVC模型的相应部分
```python
def predict(self, X):
        """
        预测部分实现
        :param X:  [n_samples, n_features]
        :return: y: ndarray, shape [n_samples,]
        """
        return self.base_estimator_.predict(X)

    def predict_proba(self, X):
        """
        预测概率部分实现
        :param X:  [n_samples, n_features]
        :return: y: ndarray, shape [n_samples,n_classes]
        """
        return self.base_estimator_.predict_proba(X)
```
#### 加载数据库并运行
```python
def load_data():
    x, y = load_iris(return_X_y=True)
    x_train, x_test, y_train, y_test = \
        train_test_split(x, y, test_size=0.3, random_state=2022)
    rng = np.random.RandomState(20)
    random_unlabeled_points = rng.rand(y_train.shape[0]) < 0.3
    y_mixed = deepcopy(y_train)
    y_mixed[random_unlabeled_points] = -1
    return x_train, x_test, y_train, y_test, y_mixed


def test_self_training():
    x_train, x_test, y_train, y_test, y_mixed = load_data()
    svm = SVC(probability=True, gamma="auto")
    # svm = SVC(probability=True)
    print(f"一共有{np.sum(y_mixed == -1)}个样本无标签.")
    model = SelfTrainingClassifier(svm, threshold=0.3)
    model.fit(x_train, y_mixed)

    y_pred = model.predict(x_train)
    print(f"模型训练结束的标志为：{model.termination_condition_}")
    print(f"模型在训练集上的准确率为: {accuracy_score(y_pred, y_train)}")

    y_pred = model.predict(x_test)
    print(f"模型在测试集上的准确率为: {accuracy_score(y_pred, y_test)}")


if __name__ == '__main__':
    test_self_training()
```
### 运行效果
接下来我们和sklearn自带的self-training对比实现的效果
#### iris
iris是一个有150个样本，四个数据维度的关于iris花分类的数据集
对iris这一较为简单的数据集进行测试，结果如下：
我们的实现
[![piYV34S.png](https://z1.ax1x.com/2023/11/14/piYV34S.png)](https://imgse.com/i/piYV34S)
sklearn自带的实现
[![piYVJ3Q.png](https://z1.ax1x.com/2023/11/14/piYVJ3Q.png)](https://imgse.com/i/piYVJ3Q)
sklearn自带的数据集可能针对自己的实现做了一些优化，使用官方文档的参数竟然达到惊人的100%准确率。
但我们的实现也不差，在这个简单的数据集上取得了97.8%的优秀准确率。
#### digits
digits是一个拥有500个样本，18个维度的手写数字图片模型，相较之下模型复杂了许多。
我们的实现
[![piYVtjs.png](https://z1.ax1x.com/2023/11/14/piYVtjs.png)](https://imgse.com/i/piYVtjs)
sklearn自带的实现
[![piYVaBq.png](https://z1.ax1x.com/2023/11/14/piYVaBq.png)](https://imgse.com/i/piYVaBq)
效果明显差了很多，但对比盲选概率的10%还是有不少的提升，至少我们接近了sklearn的实现效果。
顺便看看自监督学习的效果
[![piYV0EV.png](https://z1.ax1x.com/2023/11/14/piYV0EV.png)](https://imgse.com/i/piYV0EV)
这么看来还是不错的。
### 小结
实现了self-training的算法，感受了两个参数以及数据集中标签数量带来的影响。
可以看到self-training算法虽然简单，但是在小数据集中十分有效。
但是稍微复杂一点的数据集效果就开始打折扣了，后来测试了波士顿房地产数据集，更是惨不忍睹。
***原因是一开始的标签过少，导致分类器过拟合，并且在迭代中这个错误不会被修正，反而不断被加强。***
机器学习落实到写代码的阶段还是略有困难，即使是如此简单的算法。
日后或需要系统学习一下机器学习。
