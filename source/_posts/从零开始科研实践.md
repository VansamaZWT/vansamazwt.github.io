---
title: 从零开始科研实践之智能信息处理（一）--无标签数据
date: 2023-10-30 12:21:25
tags: 技术 机器学习
cover: https://z1.ax1x.com/2023/10/30/pimwiAP.jpg
top_image: https://z1.ax1x.com/2023/10/30/pimwiAP.jpg
---
## 前言
本学期在IIP组的王崇峻老师门下做简单的本科科研实践，以此记录学习历程。
第一次任务是对IIP组的研究方向进行概况了解，以下是科普性质的机器学习内容，如果路过的你正好对机器学习感兴趣且打算从零开始了解那本文会很适合你阅读。
# 无标签数据及其处理
## 什么是标签？
数据的标签（Label）是指对于每个数据样本所提供的预先定义的正确答案或类别信息。标签是用于训练机器学习模型的目标输出，它们提供了数据样本与相应目标之间的对应关系。
IIP的**无标签数据**指的是在智能信息处理中使用的没有预先标记或分类的数据。这类数据通常是原始的、未经处理的信息，没有附加的标签、类别或注释。与有标签数据相比，无标签数据的主要特点是缺乏明确的标识或指示。
## 如何处理无标签的数据？
所谓的监督学习就是有明确的监督信号（比如分类任务中的 label，检测任务中的 cls等）去驱动网络不断地学习更好的特征表示，并完成相应的任务。所以对于无监督学习，我们很容易想到，能不能用什么方法，去构造某些监督信号？可能这些构造出来的信号不及监督学习中的label、bbox那样明确，但在无监督大样本的加持下，总算也能让模型慢慢变好。
### 无监督学习
***无监督学习***是一种机器学习方法，其目标是从无标签数据中发现数据的内在结构、模式和关联，而无需预先提供类别标签或目标输出。与有监督学习不同，无监督学习不依赖于人工标注的数据，而是通过算法自动学习数据的特征和潜在规律。
#### 聚类
聚类是一种无监督学习的算法，是将相似的数据样本分组或聚集在一起的任务。聚类算法根据数据样本之间的相似性或距离度量，将它们划分为不同的群组，使得同一群组内的样本相似度较高，而不同群组之间的相似度较低。聚类常用于数据分析、市场细分、图像分割等领域。
##### K均值聚类
K均值聚类就是制定分组的数量为K，自动进行分组。

K 均值聚类的步骤如下：

1.定义 K 个重心。一开始这些重心是随机的（也有一些更加有效的用于初始化重心的算法）
2.寻找最近的重心并且更新聚类分配。将每个数据点都分配给这 K 个聚类中的一个。每个数据点都被分配给离它们最近的重心的聚类。这里的「接近程度」的度量是一个超参数——通常是欧几里得距离（Euclidean distance）。
3.将重心移动到它们的聚类的中心。每个聚类的重心的新位置是通过计算该聚类中所有数据点的平均位置得到的。
重复第 2 和 3 步，直到每次迭代时重心的位置不再显著变化（即直到该算法收敛）。
![](https://pic1.zhimg.com/v2-d37f29d9c9e9f3dffd46273b5f0fa15c_b.webp)
##### 层次聚类
如果不知道应该分为几类，那么层次聚类就比较适合了。层次聚类会构建一个多层嵌套的分类，类似一个树状结构。
层次聚类的步骤如下：

1.首先从 N 个聚类开始，每个数据点一个聚类。
2.将彼此靠得最近的两个聚类融合为一个。现在你有 N-1 个聚类。
3.重新计算这些聚类之间的距离。
重复第 2 和 3 步，直到你得到包含 N 个数据点的一个聚类。
选择一个聚类数量，然后在这个树状图中划一条水平线。
![](https://pic3.zhimg.com/80/v2-9a2efd705bd291566d9107cf82b1c816_720w.webp)
#### 异常检测
异常检测旨在识别与大多数数据样本不同或具有异常行为的样本。它通过对数据进行建模和分析，在不知道正常值的情况下，寻找与正常模式不符的异常点或离群值。异常检测可应用于网络安全、金融欺诈检测、设备故障检测等场景。
##### 基于高斯分布的异常检测
基于高斯分布的异常检测是一种常见的异常检测算法，也被称为正态分布异常检测。该算法的基本思想是假设正常数据集是由高斯分布生成的，而异常点则偏离了高斯分布的中心，因此可以通过计算数据点与高斯分布中心之间的距离来确定异常点。具体地，算法包括以下步骤：
1.对于数据集 X ，先求出每个特征的均值和标准差，得到一个 n 维高斯分布的概率密度函数。
2.对于一个新的数据点 X，计算其在各个特征上的概率密度值，即：
[![pimwkh8.png](https://z1.ax1x.com/2023/10/30/pimwkh8.png)](https://imgse.com/i/pimwkh8)
3.如果 p ( x ) < ϵ，则将 x  视为异常点，其中 ϵ  是阈值参数，通常由交叉验证等方法确定。

```python
import numpy as np
import matplotlib.pyplot as plt

# 生成二维数据集
np.random.seed(42)
X = np.random.randn(100, 2)
X[:5] += np.array([5, 5])  # 添加一些异常点

# 计算均值和方差
mu = np.mean(X, axis=0)
sigma = np.std(X, axis=0)

# 计算概率密度函数
p = np.prod(1 / (np.sqrt(2 * np.pi) * sigma) * np.exp(- (X - mu) ** 2 / (2 * sigma ** 2)), axis=1)

# 设置阈值
epsilon = 1e-4

# 找出异常点
outliers = X[p < epsilon]

# 绘制数据集和异常点
plt.scatter(X[:, 0], X[:, 1])
plt.scatter(outliers[:, 0], outliers[:, 1], color='r', marker='x', s=100)
plt.show()

```
![](https://img-blog.csdnimg.cn/5140fa66fd024cd2a4c4eb39bb1e0cc8.png#pic_center)
#### 关联规划
关联规则用来描述两个或多个事物之间的关联性，其通过一件或多件事物来预测其它事物，可以从大量数据中获取有价值数据之间的联系。关联规则挖掘常用于市场篮子分析、推荐系统等领域。
[![pimwV1g.png](https://z1.ax1x.com/2023/10/30/pimwV1g.png)](https://imgse.com/i/pimwV1g)
### 自监督学习
自监督学习（Self-supervised Learning）作为Unsupervised Learning的一个重要分支，给出了很好地解决方案。它的目标是更好地利用无监督数据，提升后续监督学习任务的效果。

其基本思想是：Predicting everything from everything else。

具体方法是首先定义一个Pretext task (辅助任务)，即从无监督的数据中，通过巧妙地设计自动构造出有监督（伪标签）数据，学习一个预训练模型。构造有监督（伪标签）数据的方法可以是：假装输入中的一部分不存在，然后基于其余的部分用模型预测缺失的这部分。如果学习的预训练模型能准确预测缺失部分的数据，说明它的表示学习能力很强，能够学习到输入中的高级语义信息、泛化能力比较强。而深度学习的精髓正在于强大的表示学习能力。

然后可以将预训练模型，通过简单的Finetune，应用到下游的多个应用场景，能比只使用监督数据训练的模型有更好的效果。

自监督学习的关键在于如何设计Pretext任务、损失函数，来优化预训练模型参数。
E.G.2015 (ICCV) Unsupervised Visual Representation Learning by Context Prediction

ICCV 2015这篇论文里，构造训练数据的方法是：随机从图片采样一个patch，然后从它的邻居里随机采样一个patch，监督标签对应是邻居的位置信息。作者认为，准确地预测两个patch的位置关系，需要模型学会物体整体和部分的关系。
[![pimwZcQ.png](https://z1.ax1x.com/2023/10/30/pimwZcQ.png)](https://imgse.com/i/pimwZcQ)
### 半监督学习
在许多ML的实际应用中，很容易找到海量的无类标签的样例，但需要使用特殊设备或经过昂贵且用时非常长的实验过程进行人工标记才能得到有类标签的样本，由此产生了极少量的有类标签的样本和过剩的无类标签的样例。因此，人们尝试将大量的无类标签的样例加入到有限的有类标签的样本中一起训练来进行学习，期望能对学习性能起到改进的作用，由此产生了半监督学习(Semi-supervised Learning)，如如图１所示。半监督学习(Semi-supervised Learning)避免了数据和资源的浪费，同时解决了监督学习(Supervised Learning)的 模型泛化能力不强和无监督学习（Unsupervised learning）的模型不精确等问题。
[![pimwlNV.png](https://z1.ax1x.com/2023/10/30/pimwlNV.png)](https://imgse.com/i/pimwlNV)
（1）平滑假设(Smoothness Assumption)：位于稠密数据区域的两个距离很近的样例的类标签相似，也就是说，当两个样例被稠密数据区域中的边连接时，它们在很大的概率下有相同的类标签；相反地，当两个样例被稀疏数据区域分开时，它们的类标签趋于不同。

（2）聚类假设(Cluster Assumption)：当两个样例位于同一聚类簇时，它们在很大的概率下有相同的类标签。这个假设的等价定义为低密度分离假设(Low Sensity Separation Assumption)，即分类决策边界应该穿过稀疏数据区域，而避免将稠密数据区域的样例分到决策边界两侧。

（3）流形假设(Manifold Assumption)：将高维数据嵌入到低维流形中，当两个样例位于低维流形中的一个小局部邻域内时，它们具有相似的类标签。
[![pimwuBn.png](https://z1.ax1x.com/2023/10/30/pimwuBn.png)](https://imgse.com/i/pimwuBn)
### 迁移学习
迁移学习是一种机器学习方法，旨在通过将从一个领域（源领域）学到的知识和经验应用于另一个领域（目标领域）的任务中，从而改善目标领域的性能。迁移学习的核心思想是通过利用源领域的数据，模型或特征来辅助目标领域的学习，尤其在目标领域数据有限或标注困难的情况下表现出色。
迁移学习可以归为三类：

领域（Domain）：领域是指数据的分布和特征空间。源领域和目标领域可以具有不同的数据分布，但通常假设它们在某种程度上是相关的。

知识（Knowledge）：知识是从源领域中学到的有关任务的信息，可以是模型的权重、特征表示或其他中间表示。迁移学习的目标是将这些知识迁移到目标领域中，以提高目标领域的性能。

迁移策略（Transfer Strategy）：迁移策略定义了如何利用源领域的知识来改善目标领域的学习。常见的迁移策略包括特征提取、预训练模型微调、领域自适应和样本重用等。

特征提取：将源领域的特征提取器应用于目标领域，利用共享的特征表示来学习目标领域的任务。
预训练模型微调：在源领域上预训练的模型作为初始模型，在目标领域上继续进行训练，通过微调适应目标领域的任务。
领域自适应：通过对源领域和目标领域的分布进行对齐或调整，以减小领域之间的差异，从而提高目标领域的泛化性能。
样本重用：利用源领域的有标签数据或生成的合成样本，结合目标领域的有标签数据进行训练。
迁移学习可以在许多领域中应用，如计算机视觉、自然语言处理和推荐系统等。它能够加速模型的训练过程，提高模型的性能和泛化能力，并在数据稀缺或领域变化的情况下表现出色。通过迁移学习，可以更好地利用已有的知识和数据资源，从而降低机器学习在新任务上的成本和难度。

##结尾
以上知识来源于多篇知乎，国内博客，GPT-3.5，后面迁移学习和半监督学习的部分还有待深入。